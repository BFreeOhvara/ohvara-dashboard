import { MessageCenter } from '../../components/messages/MessageCenter'

// Rep's view — message Brayden (dashboard questions) or Nate (sales
// questions), 3-panel chat layout (Prompt 12).
export default function Messages() {
  return <MessageCenter role="rep" />
}
